// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ConnectionSettings, Engine } from "./config";
import type { DatabaseKey } from "./engine";

/**
 * Ce que la modale d'export a le droit de dire, et rien d'autre.
 *
 * **Cinq verdicts, pas un booléen.** « Indisponible » recouvre cinq situations, dont deux
 * se ressemblent sans être la même chose : `NotYetSupported` est une promesse à tenir
 * (`16`–`21`), `NoLocalDump` une impossibilité de construction — Snowflake et BigQuery
 * n'ont pas d'outil local, leur export sort vers un stockage cloud, ce qui heurte
 * « aucune ressource réseau ». Les fondre dirait « pas encore » d'un cas qui ne viendra
 * jamais.
 */
export type DumpAvailability = { "kind": "ready", tool: string, version: Version, } | { "kind": "toolMissing", binary: string, } | { "kind": "toolTooOld", tool: Version, server: Version, } | { "kind": "notYetSupported", engine: Engine, } | { "kind": "noLocalDump", engine: Engine, };

/**
 * Ce qu'un échec de dump dit au front.
 *
 * **`kind` en plus du message** : la modale doit pouvoir traiter « tronqué » et « annulé »
 * autrement qu'un échec quelconque, et reconnaître un cas en cherchant un mot dans une
 * phrase française serait un couplage au libellé.
 */
export type DumpFailure = { kind: string, message: string, };

/**
 * La requête d'export ou d'import, telle que le front la fournit.
 */
export type DumpRequest = { key: DatabaseKey, variant: ConnectionSettings, engine: Engine, 
/**
 * Le chemin choisi dans le sélecteur natif — destination à l'export, source à l'import.
 */
file: string, };

/**
 * Ce que la lecture des deux extrémités permet de dire, et rien de plus.
 *
 * **Au-delà de l'en-tête et du pied, rien n'est validé** : le corps est un script SQL
 * arbitraire, et prétendre le vérifier serait mentir.
 */
export type Inspection = { "kind": "pgDump", origine: Version, } | { "kind": "tronque" } | { "kind": "tropRecent", origine: Version, cible: Version, } | { "kind": "etranger" } | { "kind": "vide" } | { "kind": "illisible", cause: string, };

/**
 * Une version de moteur ou d'outil, réduite à ce dont les règles ont besoin.
 *
 * **La mineure est portée mais ne décide de rien** : elle est affichée dans les messages
 * (« pg_dump 13.14 face à un serveur 17.6 » se lit mieux que « 13 face à 17 »), et
 * mesuré le 19 août 2026, `pg_dump` 17.4 dumpe un serveur 17.6 sans se plaindre. Une
 * règle sur la version complète refuserait donc un cas qui marche.
 */
export type Version = { majeure: number, mineure: number, };
